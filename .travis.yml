sudo: required
services:
  - docker

before_install:
  - gem install bundler --pre
  - docker build -t platof/docker-react -f Dockerfile.dev .

script:
  - docker run platof/docker-react npm run test -- --coverage
