pipeline {
    agent any
    tools {


    }
    stages {
        stage('build') {
            steps {
                echo 'install npm...'
                sh 'node --version'
		sh 'npm install'
            }
        }
    }
}
